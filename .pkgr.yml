user: openproject
group: openproject
targets:
  debian-7:
    build_dependencies:
      - libmagickwand-dev
      - libsqlite3-dev
    dependencies:
      - debconf
      - memcached
before_precompile: "./packaging/debian/setup.sh"
after_install: "packaging/debian/postinst"
addons:
  - mysql
  - apache2
  - smtp
crons:
  - extra/cron/openproject-clear-old-sessions

